import Header from '@components/header';
import CartOverview from '@components/cart/cartOverview';

const cart = () => {
    return (
        <>
            <Header />
            <CartOverview />
        </>
    );
};

export default cart;
